
p = user_signed_in? ? (link_to "Ask question", new_question_path) : ''
p Question list
- @questions.each do |q|
  .container
    .title      = link_to q.title, question_path(id: q.id)
    .body       = q.body
    .created_at = q.created_at
    br
p = user_signed_in? ? (link_to "Sign out", users_sign_out_path) : ''
